workflows:
  ios-workflow:
    name: iOS Workflow
    environment:
      flutter: stable
    scripts:
      - name: Install dependencies
        script: flutter pub get
      - name: Build iOS
        script: flutter build ios --releasegit add codemagic.yaml
  git commit -m "Add codemagic yaml"
  git push origin master