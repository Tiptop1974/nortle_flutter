workflows:
  default:
    name: iOS Release Build

    environment:
      flutter: stable

    scripts:
      - flutter pub get
      - flutter build ipa --release

    artifacts:
      - build/ios/ipa/*.ipa

